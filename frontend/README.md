# Frontend (React)

## Setup

```bash
cd frontend
yarn install
yarn start
```

## Environment

Use `frontend/.env`:

```env
REACT_APP_BACKEND_URL=http://localhost:8001
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
```
